arr = [3, 2, 5, 4, 1]

# Sort the array
arr.sort()

# The first element after sorting will be the minimum
min_val = arr[0]

# The last element after sorting will be the maximum
max_val = arr[-1]

# Print the results
print(f"Minimum element: {min_val}")
print(f"Maximum element: {max_val}")
